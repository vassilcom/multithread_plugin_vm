{
    "tasks": [
        {
            "label": "build_project",
            "type": "shell",
            "command": "cmake .. && cmake --build ${workspaceFolder}/build ",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ],
    "version": "2.0.0"
}